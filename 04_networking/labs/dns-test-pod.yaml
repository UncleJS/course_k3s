apiVersion: v1
kind: Pod
metadata:
  name: dns-test
  labels:
    app: dns-test
spec:
  containers:
    - name: test
      image: busybox:1.36
      command: ["sleep", "3600"]
  restartPolicy: Always
